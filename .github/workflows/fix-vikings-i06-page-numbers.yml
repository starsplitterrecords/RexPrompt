name: Fix Vikings Issue 6 page numbers
on:
  push:
    branches: [productionize/vikings-issue6]
    paths:
      - tools/fix_vikings_i06_page_numbers.py
      - .github/workflows/fix-vikings-i06-page-numbers.yml
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: productionize/vikings-issue6
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python tools/fix_vikings_i06_page_numbers.py
      - name: Commit page number fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/shows/vikings-2026-s1/pages_i06_p01_p06.json data/shows/vikings-2026-s1/pages_i06_p07_p12.json data/shows/vikings-2026-s1/pages_i06_p13_p18.json data/shows/vikings-2026-s1/pages_i06_p19_p24.json
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Add Vikings Issue 6 page numbers"
          git push origin HEAD:productionize/vikings-issue6
