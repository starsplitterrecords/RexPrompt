name: Probe ChatGPT share recovery

on:
  push:
    paths:
      - production/recovery/share-source.json
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Probe public share page
        shell: bash
        run: |
          python3 - <<'PY'
          import json, re, urllib.request, urllib.parse, sys
          cfg = json.load(open('production/recovery/share-source.json', encoding='utf-8'))
          url = cfg['url']
          req = urllib.request.Request(url, headers={'User-Agent':'Mozilla/5.0 (StarSplitterVisions recovery probe)'})
          try:
            with urllib.request.urlopen(req, timeout=30) as r:
              data = r.read()
              final = r.geturl()
              status = getattr(r, 'status', None)
          except Exception as e:
            print('FETCH_ERROR', type(e).__name__, str(e))
            sys.exit(1)
          print('STATUS', status)
          print('FINAL', final)
          print('BYTES', len(data))
          text = data.decode('utf-8', 'replace')
          print('HAS_OAIUSE", 'oaiusercontent' in text.lower())
          print('HAS_FILE_ID', 'file_' in text)
          print('HAS_NEXT_DATA', '__NEXT_DATA__' in text)
          print('HAS_IMAGE_TOKEN', 'image' in text.lower())
          urls = set()
          for m in re.finditer(r'https?:\/\/[^"\\'<\\\\\\s]+', text):
            u = m.group(0).replace('KÉÂÂròr’ç&WÆ6R‚uÅÇS#brÂrbr¢–bç’†²–âRæÆ÷vW"‚’f÷"²–â‚vö—W6W&6öçFVçBrÂvö––Öv–6FârÂv÷Væ’æ6öÒö&6¶VæBÖ’rÂvf–ÆW2rÂrçærrÂræ§rrÂræ§VrrÂrçvV'r’“ ¢W&Ç2æFB‡U³£SÒ¢&–çB‚t4äD”DDUõU$Å2rÂÆVâ‡W&Ç2’¢f÷"R–â6÷'FVB‡W&Ç2•³£ƒÓ ¢&–çB‚uU$ÂrÂR¢ 